// attendez que la page entière termine le chargement
window.addEventListener('load', function() {
	
	// setTimeout pour simuler le délai d’un chargement réel de page
	setTimeout(lazyLoad, 1000);
	
});
